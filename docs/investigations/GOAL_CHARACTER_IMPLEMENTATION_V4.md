# Character implementation
Authority: Overlay V4 accepted via PR29, main-derived isolated base 49ad1e4. Route REUSE / EXEC_PLAN / DURABLE. Scope and Owner authorization follow GOAL_CHARACTER_INTEGRATION_V4.md; production forbidden. Exactly two presentation recipes, existing preference record and scheduler; Engine/Pack schema/thresholds/usage/ledger untouched. Transparent assets generated in approved ChatGPT browser; deterministic split/format conversion only. Required validation and independent reviews remain pending until recorded conformance.

```text
BASE_HEAD = 49ad1e460d7585eb8a17278f6ea4dd809fc07535
CURRENT_BASE_HEAD = 49ad1e460d7585eb8a17278f6ea4dd809fc07535
ROUTE_STAGE = IMPLEMENTATION
AUTHORITY_ACCEPTED_IN_BASE = YES
PRIMARY_AUTHORITY = DSH_PET_OVERLAY_ADAPTER_V4@e2357a719b986b8c80dd41e828b8f7000560bf3d
RELATED_AUTHORITIES = CONFIGURABLE_PET_ENGINE_V4, VEHICLE_PET_PROGRESS_SOURCE_V2, DSH_USAGE_PROGRESS_SOURCE_V2
IMPLEMENTATION_AUTHORITY = contracts
EXECUTION_MANDATE = VALID (Owner Goal plus approved integration request)
MUTATION_AUTHORIZATION = VALID
ISOLATED_WRITE_SURFACE = YES (codex/character-integration-v4)
SPEC_GAP_DEPENDENCY = NONE
CONTROLLED_RUNBOOK_REQUIRED = NO (no production operation)
PRODUCTION_APPLY_ALLOWED = NO
```

Presentation geometry preserves 112/216 square resident footprints; figures and hit areas share one transform to reserve space for the descriptive label. SMALL now reserves the same known whale footprint as LARGE; previously its composer-only inset could overlap the whale. Explicit reduced motion disables shell movement transitions as well as descendant animation, including the companion's small idle/working motion. A pre-existing JavaScript-style comment inside the CSS text was converted to a CSS comment so the bubble declaration is parsed. These fixes support the affected V4 geometry/cadence obligations and introduce no host DOM probing.

Initial full web verify passed. DSH iteration found three old preference equality expectations, an SVG loader/asset-count gap in build checks and HMR test build, overlong English speech, a native JSON import attribute requirement in Playwright, and a reduced-motion menu-clamp transition race. They were repaired before final candidate verification. Intermediate test runs with source changes in progress are regression observations, not final fixed-head conformance.

Independent review closed B01 by deriving every pose hitbox from the exact PNG alpha bounds, mapped through the 320/540 portrait geometry with a two-pixel motion allowance. It closed B02 by prioritizing the shared speech surface while retaining Engine feedback dispatch and expiry. The compact menu was shortened and made two/three-column so its default above placement clears the co-installed whale. A final visual review corrected pose 9's hair crop without image painting or background manipulation. All three fixes preserve accepted contracts and shared growth state. The final behavior candidate is ca2728598039932dafeef1b3b4ea30675d482287; the final conformance record distinguishes exact-head results from intermediate diagnostic runs.
