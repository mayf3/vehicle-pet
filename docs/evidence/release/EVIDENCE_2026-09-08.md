# Goal 发布 — Evidence record (2026-09-08)

Tool: `scripts/release/vehicle-pet-release.mjs` v1.0.0 at branch
`release/helper-v1` (Base `b64e2d0`). Runbook:
`docs/runbooks/VEHICLE_PET_PRODUCTION_RELEASE_R1.md`.

## REAL_PRODUCTION_READ_ONLY_DRY_RUN = PASS

Command (run from the implementation worktree; read-only plan mode):

```text
node scripts/release/vehicle-pet-release.mjs a81809c3fbcc2b5b4b97917463bf75f862aeaee9 \
  --dsh-home ~/.dsh --profile web --out vehicle-pet-release-receipts/dry-run-official
```

Receipt: `DRY_RUN_RECEIPT_2026-09-08.json` (verbatim copy of the tool's
`receipt.json`, receiptId `2026-09-08T16-09-58-466Z-plan-a81809c3-2f6e72`).

- RESULT = `NO_OP_ALREADY_AT_TARGET` (TARGET == CURRENT == a81809c3fb…).
- Verdicts: GIT_TRUTH PASS (FETCH_BY_SHA from origin),
  CURRENT_REF_DISCOVERY PASS, SERVICE_DISCOVERY PASS (pid 81538, port
  3080), WEB_HEALTH PASS (HTTP 200), OTHER_PLUGINS_PRESERVED PASS,
  STATE_PROBE_PRE PASS (Owner browser tab matched, pet storage keys read),
  NO_OP PASS, ZERO_MUTATION PASS.
- Post-run zero-change proof: profile pin still
  `a81809c3fbcc2b5b4b97917463bf75f862aeaee9`; web leaf PID still 81538 on
  port 3080; health 200. REAL_PRODUCTION_MUTATED = NO.

## DISPOSABLE_E2E = PASS (CASE_1..6)

Full driver log: `DISPOSABLE_E2E_2026-09-08.log` (throwaway DSH home +
profile `web` + port 3088 + throwaway CDP browser; plugin installed from
the production-shaped git pin `git+https://github.com/mayf3/vehicle-pet.git#cce0e9d24…`).

- CASE_1 normal upgrade cce0e9d → a81809c: plan read-only (bytes/PID/served
  hash untouched), apply exit 0; RUNTIME_PROOF PASS at all three layers
  (lockfile resolution == a81809c3fb…; installed tree == files-projection
  of `git archive a81809c3fb…`, 151 files; served client.js == installed,
  6,857,461 bytes); CONTROLLED_RESTART PASS (port 3088 → 3088, health
  PASS); state preserved (position, reducedMotion, usage ledger 7→7
  points, engine IndexedDB); prefs survive reload post-upgrade.
- CASE_2 already-at-target a81809c → a81809c: plan and apply both report
  `NO_OP_ALREADY_AT_TARGET`; ZERO_MUTATION = YES (no install, no restart,
  no preference write, no package/lock write); service PID and bytes
  unchanged.
- CASE_3 unrelated dependency drift (package.json gained
  `drift-simulation-pet` absent from the lockfile): apply exit 3,
  `UNRELATED_DEPENDENCY_DRIFT`, phase PRE_INSTALL — stopped before any
  install or restart; vehicle-pet pin untouched; served bytes unchanged.
- CASE_4 rollback to preimage via CASE_1's receipt: exit 0; pin restored
  to cce0e9d24…; served bytes hash-identical to the pre-upgrade build;
  RUNTIME_PROOF PASS at cce0e9d24….
- CASE_5 invalid refs (`main`, `latest`, 8-hex, 39-hex, 41-hex,
  non-hex-40, unreachable 40-hex via a hermetic fixture origin): all
  `REJECT_BEFORE_MUTATION` (exit 2); profile bytes untouched.
- CASE_6 stale local main: fixture clone whose local main is one commit
  behind its origin; the tool resolved the target by FETCH_BY_SHA from
  origin and produced PLAN_READY; the stale clone's main and untracked WIP
  file were untouched.

Note (diagnostic, not a CASE assertion): the pinned f77b5a2 harness gates
plugin client activation behind full onboarding, so the V2 client does not
mount a visible pet in the disposable browser; runtime identity is proven
by the tool's three-layer RUNTIME_PROOF instead. The production harness
serves and mounts the same git-ref install (verified live in the 灵动
apply round).
