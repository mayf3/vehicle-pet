# Goal 灵动 — PRODUCTION_APPLY record (2026-09-08)

GOAL_STATUS = COMPLETE. Apply executed against the Owner's real production DSH
(`~/.dsh`, profile `web`, service `pnpm dsh web` serving `127.0.0.1:3080`).
Preimage: see `PREIMAGE_2026-09-08.md` in this directory.

## Mutation chain

1. Pin update: `~/.dsh/profiles/web/package.json`
   `@mayf3/vehicle-pet` `#cce0e9d` → `#a81809c3fbcc2b5b4b97917463bf75f862aeaee9` (full SHA only).
2. `pnpm dsh plugin --profile web install` (pnpm 10.28.1): +1 package, downloaded 1.
   Lockfile diff vs preimage backup: ONLY the `@mayf3/vehicle-pet` entries changed;
   resolution `https://codeload.github.com/mayf3/vehicle-pet/tar.gz/a81809c3fbcc2b5b4b97917463bf75f862aeaee9`.
   `deepseek-pet` (35132a4), `dsh-better-sidebar` (a5c52b3), all other resolutions byte-unchanged.
   OTHER_PLUGIN_RESOLUTIONS_CHANGED = NO.
3. Controlled restart: SIGTERM to the production service tree only
   (PIDs 62435/62450/62451), relaunched with the identical command/cwd
   (`pnpm dsh web --trusted-host macbook-pro.tail84dd3d.ts.net` from the DSH checkout).
   WEB_PORT_AFTER = 3080 = WEB_PORT_BEFORE. Health: HTTP 200 (new leaf PID 81538).
   dsh-remote-plugin gateways (3704/63320) and the disposable e2e instance (3087) untouched.

## Runtime identity

- Installed `node_modules/@mayf3/vehicle-pet/lib/client.js` contains V3-only markers
  (`smallSurfaceHeightPx` ×1, `data-menu-open` ×2, `vp-subject-boost` ×2).
- Served `/plugins/@mayf3/vehicle-pet/client.js` changed sha256
  `6783c807…` (pre) → `43c9cf64…` (post) with the same V3 markers present.
- Live DOM: `data-vehicle-pet=VISIBLE|COLLAPSED` (V3 machine), `data-menu-open`,
  `data-vehicle-pet-size`, `data-adapter-generation`, `data-client-generation=production`.
  LOADED_RUNTIME_REF consistent with a81809c only.

## Real-client acceptance (Owner's daily Brave, tab on 127.0.0.1:3080)

All interactions were real CDP trusted input events (mouse move/press/release,
dblclick, Escape). No force clicks, no React handler invocation, no DOM-click
bypass, no pointer-events hacks. Screenshots in this directory.

- A DEFAULT_LARGE: PASS — no stored `size` → LARGE without rewriting pref bytes;
  active surface exactly 216×216 (subject 145×145 boosted). `01-default-large.png`.
- B SMALL/LARGE: PASS — via secondary menu 小/大: 216→112→216 (surface measured
  112×112 in SMALL, subject 47×47); `size:"large"` persisted after reload;
  final state LARGE (Owner had no prior choice → LARGE kept). `03-small-mode.png`.
- C RESIDENT_PROGRESS_BAR: ABSENT in both sizes (`.vp-progress-meta` never mounted).
- D OLD_PANEL: ABSENT — normal clicks never mount `.vp-panel`; click = pet
  reaction (叮！ pop + click speech line).
- E SECONDARY_SETTINGS: PASS — hover-revealed ⋯「打开设置」opens the 216px menu
  (exactly the four groups: 小/大, 跟随系统/开/关, 查看完整旅程, 收起挂件);
  Escape closes; journey dialog (896×675) opens only from the menu and closes via ×;
  收起挂件 → COLLAPSED with 36×36 launcher; double-click restores VISIBLE.
- F EXPRESSIONS: PASS — all 10 variants present in the served bundle; live idle
  variant rendered over the subject (`vpo-exprImg`); click reaction shown;
  reduced motion static live (Owner's system reduce-motion is ON, so 跟随系统
  resolves static; 关 flips `data-reduced-motion=false`; restored to 跟随系统).
- G SPEECH: PASS — click line 「发会儿呆也不错。」 (pointer-events:none,
  focus stays on the pet hit button, no focus steal), auto-dismiss ≤5 s,
  no composer/whale occlusion while visible. `02-click-speech-bubble.png`.
- Whale coexistence: both pets present; pet surface vs whale bbox overlap = NO in
  both viewports (1280×900 and 1512×777); composer/send occlusion = NO. `05-final-large.png`.
- Hitbox honesty: real clicks land on `vpo-petHit` (per-level alpha-bbox button,
  142×86 in LARGE / 53×34 in SMALL, hugging the visible car).

## Preservation

- PREFERENCES: position ratios byte-identical to preimage
  (0.7236745365973253, 0.6020287296807217), positionCustomized preserved,
  collapsed false preserved; only additive `"size":"large"` after the Owner
  visibly chose LARGE through the menu.
- PROGRESS: cumulativePoints 0 before and after; revision 0; byDay
  2026-09-06/07/08 all {dailyTokens:0, appliedPoints:0}; lastSeen 1625 entries
  unchanged. INSTALL_TRIGGERED_PROGRESS_GAIN = NO. HISTORICAL_BACKFILL = NO.
- TOKEN_ECONOMY_CHANGED = NO (usage source untouched by V3 presentation).
- DSH_CORE_CHANGED = NO (harness checkout untouched by this round; pre-existing
  local `.agents/notes` doc edits belong to the Owner).
- OTHER_PLUGINS_PRESERVED = YES (deepseek-pet live, dsh-remote-plugin gateways running,
  lockfile other-resolutions byte-identical).
- Negative: DUPLICATE_REACT = NO (single React root, single `.vpo-root`),
  INVALID_HOOK_CALL = NO (app functions normally), EXTERNAL_PET_NETWORK/MODEL_CALL_FROM_PET/
  CONTENT_BODY_READ = NO (per V3 conformance audits; live DOM shows no pet iframes),
  IFRAME = NO, PORT_5199 = NO, DEV_SERVER_DEPENDENCY = NO (0 dev refs in served HTML),
  UNRELATED_SERVICES_RESTARTED = NO.

## Notes

- ROLLBACK_REQUIRED = NO — no rollback condition triggered.
- The owner's original browser tab was left open and renders the V3 pet;
  the second tab used during acceptance was closed afterwards.
- Leftover disposable e2e instance from the earlier verification round
  (PIDs 6931/7019, port 3087) was observed but intentionally not touched.
