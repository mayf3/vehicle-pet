# Overlay V6 compact playful resident

Base: `74b8f645ff798c9752322b214917144dc9e5de30` (accepted V6, PR33).
Code/bundle candidate: `7fdc564a36a96be2ded2b0186ab1d8b8d345b7a6`.
Production bundle SHA256: `951e4509900a4db788b1927a7ac3638ff95229fc0e022885160469637cf31ed8`.
Route: [implementation brief](../investigations/OVERLAY_V6_IMPLEMENTATION.md).

Vehicle captions use the complete source-alpha bottom through the existing scale, plus 8px. The grade and active-session footer flow together with a 4px gap, including wrapped English descriptions at small size. Companion caption placement stays unchanged. Original Engine, Pack/thresholds, usage source and image bytes are unchanged.

One transient adapter reaction uses eight combinations of approved expressions/poses, pointer-inert decoration and bounded motion: wave, wink, bounce, peek, shy, sleepy, proud, nod. These are eight reactions, not eight newly drawn poses. Single click cycles; eligible shared ambient speech advances the same pool. Real state takes priority. Effects expire after 2400ms, motion ends within 1200ms, and menu/drag/hide/character switch cancel. OS reduce is live and latched for the current reaction so restoring motion cannot replay it. Figure and hit target move together while text remains stationary. No new recurring timer, model call, motion setting, persistence or progress mutation.

## Evidence

- 135 DSH unit/DOM tests PASS, including eight-reaction reachability, one timer, expiry/replacement, state priority/no historical completion replay, blocked menu/drag/hidden interactions, character cancellation, ambient key deduplication, live reduce→restore non-replay and disposal. HMR inventory verifies the new visibility listener is removed.
- `pnpm verify` PASS: 158 unit/DOM, 12 prototype browser, 18 contracts, typecheck/lint/build and original asset checks. This development run preceded only the OS latch repair, flow-caption repair and focused-test additions; final affected checks are recorded separately.
- Focused real-browser alpha matrix and reaction run PASS before final capture cleanup: all 12 levels × 2 sizes measured from decoded actual image alpha (>8), 6–14px to grade and 4–8px to footer; eight reactions for each exclusive character; OS static parity, allowed motion, stationary label, expiry and double-click menu. Final test Head `57cd395d4c242890e2decc06b23a3762d50bb4ce` rerun PASS; clean captures include complete-window vehicle/companion footers.
- Independent code/bundle review ACCEPT at the candidate: 65 embedded sourcemap sources match fixed Head; bundle hash agrees; blocker union empty. Reviewer did not author or build the candidate.

Pinned Harness: `f77b5a2fcebc2d9138f6608a60636f2294868d42`; disposable main test home on 3081. Existing 3085 preview home was preserved and its plugin link changed to a detached `vehicle-pet-overlay-v6-preview` worktree at the code candidate, protecting the user's preview from E2E fixture builds. Its known read-only process was restarted and CUA verified vehicle/companion, double-click menu, compact Chinese car caption and whale coexistence. No test prompt was sent in the user's preview. Actual daily 3080 activation is a separate controlled operation after final readiness.

Final regression reconciliation: first `verify:dsh` reached 39 browser PASS, one responsive-layout assertion failure, one deliberately interrupted usage test. The first read its bounding box immediately after viewport resize, before ResizeObserver; e00c00e adds a bounded poll. The usage test inherited the earlier scripted approval scenario (confirmed by its after-1 screenshot) and waited for an unavailable ordinary composer; 57cd395 resets the mock before each usage turn. Neither repair changes product code. Fixed-Head rerun `--grep 'OVERLAY_V5|V6_COMPACT_PLAYFUL|real DSH usage'` PASS 4/4 (3.0m), including both initial failures, fresh clean V6 matrix and background-session retention. Real counted usage increased 26→52 tokens without progression regression. Final typecheck/lint, 135 DSH unit/DOM and original character bytes PASS. Do not describe the interrupted first full command as PASS.

Final coexistence PASS 2/2 (31.3s), both real installed pets and companion sizes/description/footer clear whale, composer and send. Home `/tmp/vehicle-pet-overlay-v6-coexist-home`, port3086; the user's3085 preview was not recycled. Final package/bundle/lifecycle checks and governance byte validation PASS.

Independent visual review ACCEPT (code7fdc564, tests57cd395), mandatory visual fixes zero. All16 combinations plus representative12-level/size captures reviewed; clean full-window images close the earlier cropped-footer evidence gap. Final coexistence images are archived alongside them. Eight combinations reuse approved poses; no claim of eight newly painted assets. Images prove appearance; executed assertions establish lifecycle and motion.
